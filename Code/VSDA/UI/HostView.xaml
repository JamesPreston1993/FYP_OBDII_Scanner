<Page
    x:Class="VSDA.UI.HostView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VSDA.UI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <SplitView x:Name="SideMenu" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" DisplayMode="CompactInline">
        <SplitView.Pane>
            <StackPanel x:Name="ModuleIcons" Background="Gray" Orientation="Vertical">
                <Button VerticalAlignment="Center" x:Name="HamburgerButton" FontFamily="Segoe MDL2 Assets" Content="&#xE700;" Width="50" Height="50" Background="Transparent" Foreground="Black" Click="HamburgerButton_Click" />
            </StackPanel>
        </SplitView.Pane>
        <StackPanel>
            <Grid VerticalAlignment="Top" HorizontalAlignment="Stretch" Height="50" Background="LightGray">
                <TextBlock x:Name="TitleBar" HorizontalAlignment="Left" VerticalAlignment="Center" FontSize="24" Padding="10,0,0,0" TextWrapping="Wrap" Text="Module Name"/>
            </Grid>
            <Grid x:Name="ModulePage"  HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />
        </StackPanel>
    </SplitView>
</Page>
